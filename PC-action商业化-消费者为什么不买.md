# PC-action 商业化分析：消费者为什么不买 + 优化建议

> 基于已读代码现状（登录/VIP/Supabase 体系已成型，但前端无付费墙）与 PC-action vs AutoHotkey 定位分析整理。
> 目标：在动手做付费系统前，先想清楚"凭什么让人掏钱"，避免做成"功能齐全但没人买"。

---

## 一、已验证的护城河（卖点必须围绕这些讲）

| 能力 | 代码支撑 | 对手对比 |
|------|----------|----------|
| 零代码可视化录制回放 | app.py 步骤表 action_type 可逐条改 | AHK 需写脚本 |
| **看图容错回放（核心）** | image_recognition.py：OpenCV matchTemplate + 动态阈值 + 灰度快筛 + CUDA + ROI 局部加速 + turbo 闪匹配 | AHK 的 ImageSearch 是像素级精确匹配，换皮肤/改分辨率/DPI 缩放直接失效 |
| 可视化条件分支 + 跳转 + else + 多流程循环 | app.py 3805-4072 / 10778-11086 | AHK 有但需手写 |
| 倍速回放 + turbo 极速 | app.py 倍速/turbo | AHK 需手改 Sleep |

**结论**：真护城河 = **"给不会写代码的人用的、抗界面变化的看图自动化"**。对标不是 AHK，是"要重复操作电脑但不会编程的人"。该人群已有按键精灵 / Power Automate / UiPath（免费或成熟），差异化必须押在**识别容错**上，而不是功能数量。

---

## 二、消费者不买的 6 大阻力（按致命程度排序）

### 阻力 1：启动即报毒，信任瞬间崩塌（最致命）
- **现状**：启动强制 UAC 提权 + 全局键鼠钩子 + 持续截屏（image_recognition 必需）。这三个行为在任意杀软眼里都是"键盘记录器/远控"特征，必然弹"已阻止可能有害的应用"。
- **后果**：用户还没看到功能就被"病毒警告"吓退，付费意愿归零。这是比功能、定价都靠前的一道坎。
- **为什么难但必须做**：PyInstallr 单文件 + 无代码签名 → 100% 报毒。代码签名证书（~¥800/年）+ 申请杀软白名单是必要成本。

### 阻力 2：免费替代触手可及，且"够用"
- 按键精灵（免费、国内认知度高、海量现成脚本）、Power Automate（Win10/11 自带免费）、UiPath（免费社区版）。
- 普通用户的"重复操作"大多能被这三个免费解决，**只有"界面会变、靠坐标会失效"的场景**才是 PC-action 的必杀技——但这个场景用户自己意识不到，需要被教育。

### 阻力 3：卖点藏在"录制回放"里，没被看见
- 用户看到的是"又一个录制工具"，看不到"抗界面变化"的差异。AHK 的 ImageSearch 失败经历让用户默认"看图点击都不靠谱"。
- **必须先讲清楚**："别人家的看图点击换皮肤就废，PC-action 认的是图不是坐标"。

### 阻力 4：付费墙体验差（本次要补的）
- 现状：VIP 数据模型在，但**前端无价格、无购买入口、无激活流程**——等于"有会员制度但没收银台"。
- 用户即便想买，也找不到入口。本次付费系统落地就是补这道：定价（9.9/100）→ 单渠道展示 → 卡密激活。

### 阻力 5：定价与价值感知错位
- "9.9 / 100 两档"里，**100 是什么？** 如果 100 只是"更贵的功能相同"，用户不会买；必须是**明确的时长/权益差异**（建议：9.9=包月，100=包年≈8.3/月，立省 19%）。
- 个人开发者定价心理锚点：9.9 是"一顿饭"门槛，降低决策；年付折扣拉 LTV。

### 阻力 6：渠道/获客弱，没有"下单的地方"
- 现状：无官网、无安装包、PC-Action.spec 残缺（datas/hiddenimports 为空）、无 README、无教程视频。
- 用户连"在哪下载、怎么装"都不知道，更别说付费。单渠道（如爱发电/发卡网）必须挂在显眼入口 + 配套 3 分钟上手视频。

---

## 三、对应优化建议（按优先级）

| 优先级 | 阻力 | 优化动作 | 成本 |
|--------|------|----------|------|
| P0 | 报毒 | 代码签名证书 + 杀软白名单申请 + 启动页明确"为何需要权限"说明 | 中（¥+时间） |
| P0 | 卖点不显 | 首屏一句话价值主张："认图不认坐标，界面变了也能点" + 对比动图 | 低 |
| P0 | 付费墙体验 | 本次落地：定价配置 + 会员/升级入口 + 卡密激活 | 中（本次做） |
| P1 | 定价错位 | 9.9 包月 / 100 包年（明确时长差异，年付立省） | 低（配置即可） |
| P1 | 渠道弱 | 单渠道入口（爱发电/发卡网可配）+ 3 分钟上手视频 + 官网/落地页 | 中 |
| P1 | 免费替代 | 强化"看图容错"教育文案 + 提供 3 个"坐标会失效、看图不会"的现成 demo 流程 | 低 |
| P2 | 安装包 | 修 PC-Action.spec + 出一键安装包 + README + 使用帮助页补"付费说明" | 中 |

---

## 四、与本次付费系统落地的关联

本次要落地的三块（定价配置 / 卡密激活 / 单渠道可配）正好解决**阻力 4**，并为阻力 5、6 提供配置位（价格、渠道名、渠道链接都在 `pricing.json` 里，作者后台可改）。

但要注意：**付费系统只是"收银台"，不能替代 P0 的信任与卖点问题**。如果报毒不解决、卖点不讲清，收银台建好也没人进。建议付费系统上线同时，并行推 P0 的代码签名与首屏价值主张。

**付费墙策略（已确认）**：限时全功能试用（默认 7 天）→ 到期全功能锁定，提示升级。这样既给体验机会（对抗阻力 2"免费够用"），又设转化节点。激活用卡密（用户在发卡平台付款→拿码→程序内输码自动开通 VIP），个人主体合规、不接支付 API。
